{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start": "lerna run start --parallel",
    "build": "lerna run build --npm-client=npm"
  },
  "dependencies": {
    "codefield": "^2.0.4"
  }
}
